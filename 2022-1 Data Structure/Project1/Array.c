#include<stdio.h>
#include<stdlib.h>

struct node {
	int data;
	struct node* next;
};

void delete() {
	struct node* temp;
	int t;
	if (head == NULL) {

	}
}

int main() {
	struct node* head, * temp,* cursor;

	

	for(cursor=head;cursor !=NULL;cursor=cursor->next)
	
		
}


